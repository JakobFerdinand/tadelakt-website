# tadelakt.at (Astro)

This is the primary site for tadelakt.at, implemented in Astro and built as static output to `dist/`.

## Commands

From the repo root:

```sh
pnpm install
pnpm dev
```

Build + preview:

```sh
pnpm build
pnpm preview
```
